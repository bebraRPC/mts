kafka:
  brokers:
    - localhost:9092
  topic: image-topic
  group_id: worker-group

minio:
  endpoint: localhost:9000
  access_key: minio-access-key
  secret_key: minio-secret-key
  bucket_name: mts